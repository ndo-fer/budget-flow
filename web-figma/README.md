
  # Budget Flow App Idea

  This is a code bundle for Budget Flow App Idea. The original project is available at https://www.figma.com/design/e6UUBY0srpvRnl3yBsN0cx/Budget-Flow-App-Idea.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  